apiVersion: v1
kind: ConfigMap
metadata:
  name: enrichment-config
  labels:
    {{- include "enrichment-api.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
